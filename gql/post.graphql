query post($postId: ID!) {
    post(postId: $postId) {
      author {
        id
        username
        isCurrentUser
        title
      }
      commentCount
      createdAt
      editedAt
      domain
      endorsementCount
      id
      isEndorsed
      link
      textContent
      thumbnailUrl
      title
      type
      topics {
        capitalizedName
        name
      }
      newCommentCount
    }
  }
