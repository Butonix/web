query user($username: String!) {
  user(username: $username) {
    id
    username
    commentCount
    postCount
    endorsementCount
    createdAt
    lastLogin
    isBlocking
    isBlocked
    isCurrentUser
    tag
    tagColor
    profilePicUrl
    bio
  }
}
