query user($username: String!) {
  user(username: $username) {
    id
    username
    commentCount
    postCount
    endorsementCount
    createdAt
    isBlocking
    isBlocked
    isCurrentUser
  }
}
