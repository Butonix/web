query user($username: String!) {
  user(username: $username) {
    id
    username
    commentCount
    postCount
    endorsementCount
    createdAt
    lastLogin
    isBlocking
    isBlocked
    isFollowing
    isFollowed
    isCurrentUser
    tag
    tagColor
    profilePicUrl
    bio
    appearOffline
    moderatedPlanets {
      name
      description
      cardImageUrl
      avatarImageUrl
      themeColor
    }
  }
}
