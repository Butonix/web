query user($username: String!) {
  user(username: $username) {
    username
    commentCount
    postCount
    endorsementCount
    followerCount
    createdAt
    isFollowing
    isBlocking
    isBlocked
    isFollowed
    isCurrentUser
  }
}
