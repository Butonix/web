query postComments($postId: ID!, $sort: Sort) {
  postComments(postId: $postId, sort: $sort) {
    createdAt
    endorsementCount
    id
    isEndorsed
    textContent
    postId
    parentCommentId
    author {
      id
      username
      isCurrentUser
    }
  }
}
