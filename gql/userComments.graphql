query userComments($username: String!) {
  userComments(username: $username) {
    __typename
    textContent
    createdAt
    editedAt
    endorsementCount
    id
    isEndorsed
    postId
    post {
      id
      title
    }
    author {
      id
      username
      isCurrentUser
      title
    }
  }
}
